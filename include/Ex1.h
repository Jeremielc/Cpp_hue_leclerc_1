#include <iostream>
#include <cstdlib>

namespace nok {
    void exchange(int, int);
}

void fillTable(int*);
void showTable(int*);
void exchange(int*, int*);
void exchange(int&, int&);
int& findMini(int*);
int& findMaxi(int*);
int& findRand(int*);
void randomize(int*);
